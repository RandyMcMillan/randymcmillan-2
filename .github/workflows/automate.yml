name: automate.yml

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:

    branches:       #[ master ]
      - '*'         # matches every branch that doesn't contain a '/'
      - '*/*'       # matches every branch containing a single '/'
      - '**'        # matches every branch
      - 'master'    # includes master
      - 'main'      # includes main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:

  printenv:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-20.08, macos-10.15, macos-latest, arm, self-hosted]
        python-version: ['3.8', '3.9']
        exclude:
          - os: ubuntu-latest
            python-version: ['2.7','3.6','3.7']
          - os: ubuntu-20.08
            python-version: ['2.7','3.6','3.7']
          - os: macos-latest
            python-version: ['2.7','3.6','3.7']
          - os: macos-10.15
            python-version: ['2.7','3.6','3.7']
          - os: windows-latest
            python-version: ['2.7','3.6','3.7']
    steps:
      - uses: styfle/cancel-workflow-action@0.8.0
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v2
      - name: Setup Matrix
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Print github workspace
        run: |
          printenv
          make init

  automate:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-20.08, macos-10.15, macos-latest, arm, self-hosted]
        python-version: ['3.8', '3.9']
        exclude:
          - os: ubuntu-latest
            python-version: ['2.7','3.6','3.7']
          - os: ubuntu-20.08
            python-version: ['2.7','3.6','3.7']
          - os: macos-latest
            python-version: ['2.7','3.6','3.7']
          - os: macos-10.15
            python-version: ['2.7','3.6','3.7']
          - os: windows-latest
            python-version: ['2.7','3.6','3.7']
    steps:
      - uses: styfle/cancel-workflow-action@0.8.0
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v2
      - name: Setup Matrix
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: make dotfiles
        run: |
          make dotfiles
      - name: automate.sh matrix
        run: |
          python -c "import sys; print(sys.version)"
          ./automate.sh
          make init

  bitcoin-test-battery:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-20.08, macos-10.15, macos-latest, arm, self-hosted]
          #python-version: ['3.8', '3.9']
        python-version: ['3.9']
        exclude:
          - os: arm
            python-version: ['2.7','3.7','3.8', '3.9']
          - os: ubuntu-latest
            python-version: ['2.7','3.6','3.7']
          - os: ubuntu-20.08
            python-version: ['2.7','3.6','3.7']
          - os: macos-latest
            python-version: ['2.7','3.6','3.7']
          - os: macos-10.15
            python-version: ['2.7','3.6','3.7']
          - os: windows-latest
            python-version: ['2.7','3.6','3.7']
    steps:
      - uses: styfle/cancel-workflow-action@0.8.0
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v2
      - name: Setup Matrix
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: make dotfiles
        run: |
          make dotfiles
      - name: automate.sh matrix
        run: |
          python -c "import sys; print(sys.version)"
          ./bitcoin-test-battery.sh
          make init


  bitcoinand-u18-host:
    runs-on: ubuntu-18.04
    steps:
      ## REF: https://githubmemory.com/repo/Gisleburt/cancel-workflow-action
      - uses: styfle/cancel-workflow-action@0.8.0
        with:
          access_token: ${{ github.token }}

      - uses: actions/checkout@v2
      - name: Make
        run: |
          git clone https://github.com/BitcoinAnd/bitcoinAnd.github.io.git ~/bitcoinAnd && pushd ~/bitcoinAnd
          sudo make install
          sudo npm run-script build
          brew install youtube-dl ffmpeg
          make youtube-dl

  bitcoinand-macos-host:
    runs-on: macos-10.15
    steps:
      ## REF: https://githubmemory.com/repo/Gisleburt/cancel-workflow-action
      - uses: styfle/cancel-workflow-action@0.8.0
        with:
          access_token: ${{ github.token }}

      - uses: actions/checkout@v2
      - name: Make
        run: |
          git clone https://github.com/BitcoinAnd/bitcoinAnd.github.io.git ~/bitcoinAnd && pushd ~/bitcoinAnd
          sudo make install
          sudo npm run-script build
          brew install youtube-dl ffmpeg
          make youtube-dl


  pifs-u18-host:
    runs-on: ubuntu-18.04
    steps:
      ## REF: https://githubmemory.com/repo/Gisleburt/cancel-workflow-action
      - uses: styfle/cancel-workflow-action@0.8.0
        with:
          access_token: ${{ github.token }}

      - uses: actions/checkout@v2
      - name: Make
        run: |
          git clone https://github.com/RandyMcMillan/pifs.git ~/pifs && pushd ~/pifs
          sudo apt install autotools-dev
          sudo apt install automake
          sudo apt install fuse-emulator-gtk  # version 1.5.7+dfsg1-3, or
          sudo apt install gcc
          sudo apt install pkg-config
          sudo apt install libfuse-dev

          ./autogen.sh
          ./configure
          make
          make install
          mkdir -p ~/meta
          mkdir -p ~/mountpoint
          πfs -o mdd=~/meta ~/mountpoint

